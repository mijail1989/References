{% extends 'base.html.twig' %}

{% block title %}Reference{% endblock %}

{% block body %}
    <h1>Reference</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reference.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ reference.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ reference.url }}</td>
            </tr>
            <tr>
                <th>Lang</th>
                <td>{{ reference.lang }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reference.description }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ reference.img }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reference_index') }}">back to list</a>

    <a href="{{ path('app_reference_edit', {'id': reference.id}) }}">edit</a>

    {{ include('reference/_delete_form.html.twig') }}
{% endblock %}
